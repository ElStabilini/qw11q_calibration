platform: qw11q

targets: [D1]

actions:

  - id: rabi
    operation: rabi_amplitude_signal
    parameters:
      min_amp_factor: 0
      max_amp_factor: 2
      step_amp_factor: 0.1
      pulse_length: 40
      relaxation_time: 100_000
      nshots: 1024